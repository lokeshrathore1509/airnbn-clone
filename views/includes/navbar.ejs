<nav class="navbar navbar-expand-md custom-navbar sticky-top">
  <div class="container">
    <!-- Brand Logo -->
    <a class="brand-logo" href="/listings">
      <i class="fa-regular fa-compass brand-icon"></i>
      <span>wanderlust</span>
    </a>

    <!-- Mobile Toggler -->
    <button class="navbar-toggler border-0 shadow-none" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <!-- Search Bar Capsule (Center) -->
      <div class="mx-auto my-2 my-md-0">
        <form class="search-capsule" role="search" method="GET" action="/listings">
          <input 
            class="search-inp" 
            type="search" 
            name="search" 
            placeholder="Search destinations, cities, categories..." 
            value="<%= typeof searchQuery !== 'undefined' ? searchQuery : '' %>"
          />
          <button class="search-btn" type="submit" aria-label="Search">
            <i class="fa-solid fa-magnifying-glass"></i>
          </button>
        </form>
      </div>

      <!-- Right Navigation Items -->
      <div class="navbar-nav align-items-center gap-2">
        <a class="host-btn" href="/listings/new">
          <i class="fa-solid fa-plus me-1 text-danger"></i> Airbnb your home
        </a>

        <% if (!currUser) { %>
          <div class="d-flex align-items-center gap-2">
            <a class="btn btn-outline-dark rounded-pill px-3 py-1 btn-sm fw-semibold" href="/login">Log in</a>
            <a class="btn btn-dark rounded-pill px-3 py-1 btn-sm fw-semibold" href="/signup">Sign up</a>
          </div>
        <% } else { %>
          <div class="dropdown">
            <div class="user-pill-dropdown" data-bs-toggle="dropdown" aria-expanded="false">
              <i class="fa-solid fa-bars text-secondary"></i>
              <div class="user-avatar-badge">
                <%= currUser.username ? currUser.username.charAt(0).toUpperCase() : 'U' %>
              </div>
            </div>
            <ul class="dropdown-menu dropdown-menu-end shadow border-0 rounded-4 mt-2 p-2" style="min-width: 200px;">
              <li class="px-3 py-2 border-bottom mb-1">
                <span class="d-block small text-muted">Signed in as</span>
                <span class="fw-bold text-dark text-truncate d-block"><%= currUser.username %></span>
              </li>
              <li>
                <a class="dropdown-item rounded-3 py-2" href="/listings/new">
                  <i class="fa-solid fa-house-chimney me-2 text-danger"></i>Create New Listing
                </a>
              </li>
              <li>
                <a class="dropdown-item rounded-3 py-2" href="/listings">
                  <i class="fa-solid fa-compass me-2 text-primary"></i>Explore Listings
                </a>
              </li>
              <li><hr class="dropdown-divider my-1"></li>
              <li>
                <a class="dropdown-item rounded-3 py-2 text-danger fw-semibold" href="/logout">
                  <i class="fa-solid fa-arrow-right-from-bracket me-2"></i>Log out
                </a>
              </li>
            </ul>
          </div>
        <% } %>
      </div>
    </div>
  </div>
</nav>